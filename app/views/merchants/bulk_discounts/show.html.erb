<center><h1><%= @discount.name %></h1><center>

<p>Percentage off: <%= number_to_percentage(@discount.percentage * 100, precision: 0 ) %></p>
<p>Item Threshold: <%= @discount.item_threshold %></p>
<%= button_to "Edit #{@discount.name}", edit_merchant_bulk_discount_path(@merchant, @discount), method: :get, data: {turbo: false} %>